<div class="container-fluid">

  <h2 class="text-center">Add New Event</h2>

  <div class="row">
    <div class="col-3">
    </div>
    <div class="col-6">

    <% if @event.errors.any? %>
      <hr>
      
      <div style="color:red;">
        <h5><%= @event.errors.count %> errors found.</h5>
        <ul>
          <% @event.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

      <hr>
      <%= render partial: "form", locals: { event: @event, categories: @categories, submit_btn: "Create Event" } %>
      
    </div>
    <div class="col-3">
    </div>
  </div>
    
</div>
